% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{longest_paths}
\alias{longest_paths}
\title{The \code{n} longest paths, each peeled off before the next is sought.}
\usage{
longest_paths(parents, n, weights = NULL, min_length = NULL)
}
\arguments{
\item{parents}{Integer vector of 0-based parent indices (roots are \verb{< 0}).}

\item{n}{Integer; how many paths to take.}

\item{weights}{Optional numeric vector of child-to-parent edge weights; \code{NULL}
counts edges.}

\item{min_length}{Optional numeric; stop once the next path is no longer than
this. Note it measures the path's whole \emph{catchment} — every edge whose parent
lies on the path, so each twig hanging off it contributes its first edge too —
and that hitting it \strong{stops} the search rather than skipping one path. Both are
inherited from \code{navis::split_into_fragments}, where they are load-bearing.}
}
\value{
List of up to \code{n} integer vectors of 0-based node indices, longest first,
each distal-first.
}
\description{
The \code{n} longest paths, each peeled off before the next is sought.
}
