% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{contract_vertices}
\alias{contract_vertices}
\title{Contract nodes onto new ids, returning the simplified edge list.}
\usage{
contract_vertices(edges, mapping, threads = NULL)
}
\arguments{
\item{edges}{Integer or numeric \verb{(E, 2)} matrix of edges (0-based node indices).}

\item{mapping}{Integer vector giving the new 0-based id of each old node. Ids need
not be contiguous, but the output is only as compact as the ids you supply.}

\item{threads}{Optional integer; number of threads. \code{NULL} uses all cores.}
}
\value{
Integer \verb{(n_unique, 2)} matrix of the surviving edges as \verb{[min, max]} rows.
}
\description{
Both endpoints of every edge are pushed through \code{mapping}; edges that end up with
both ends on the same new node are dropped, and the rest deduplicated. This is
\code{igraph::contract_vertices()} followed by \code{simplify()}, fused — and, unlike that
pair, it does not rewrite a graph object in place, so contracting costs no copy.
}
