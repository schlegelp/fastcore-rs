% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{geodesic_predecessors}
\alias{geodesic_predecessors}
\title{Shortest path trees over a graph — distances \emph{and} the route to each node.}
\usage{
geodesic_predecessors(
  edges,
  n_nodes,
  weights = NULL,
  directed = FALSE,
  sources = NULL,
  limit = NULL,
  threads = NULL,
  precision = 32
)
}
\arguments{
\item{edges}{Integer or numeric \verb{(E, 2)} matrix of edges (0-based node indices).}

\item{n_nodes}{Integer; total number of nodes.}

\item{weights}{Optional numeric vector, one length per edge; \code{NULL} counts edges.
Must be finite and non-negative. \strong{Zero weights are explicitly allowed} — they
are how a penalised-path search makes an already-extracted route free to
re-traverse.}

\item{directed}{Logical; if \code{TRUE} an edge \verb{(u, v)} may only be traversed from \code{u}
to \code{v}.}

\item{sources}{Optional integer vector of 0-based source nodes, one tree each;
\code{NULL} uses every node.}

\item{limit}{Optional numeric; ignore nodes further away than this.}

\item{threads}{Optional integer; number of threads. \code{NULL} uses all cores.}

\item{precision}{Integer; 32 or 64, the width distances are accumulated at. The
result is numeric either way — R has no float32 — so this buys accuracy.}
}
\value{
List with \code{distances} (numeric \verb{(n_sources, n_nodes)} matrix, \code{-1} where
unreachable) and \code{predecessors} (integer matrix of the same shape giving, per
node, the node before it on the shortest path back to that row's source; \code{-1} for
the source itself and for unreachable nodes, so one \verb{>= 0} test both walks the
path and terminates it).
}
\description{
The predecessor-returning counterpart to \code{geodesic_matrix_graph}. Use this when you
need the path itself; use \code{geodesic_matrix_graph} when the distance is enough, and
\code{geodesic_path} when you want the node sequences rather than the raw chains.
}
\details{
Because this takes a bare edge list there is no index to build or invalidate
between calls, which is what algorithms that re-weight the graph every iteration
need — TEASAR zeroes the edges along each path it extracts, then searches again.
}
