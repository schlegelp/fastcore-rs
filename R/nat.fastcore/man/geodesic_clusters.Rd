% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{geodesic_clusters}
\alias{geodesic_clusters}
\title{Greedily partition nodes into connected clusters of bounded radius.}
\usage{
geodesic_clusters(edges, n_nodes, max_dist, weights = NULL, seeds = NULL)
}
\arguments{
\item{edges}{Integer or numeric \verb{(E, 2)} matrix of edges (0-based node indices).
Treated as undirected.}

\item{n_nodes}{Integer; total number of nodes. Isolated nodes each become their own
cluster.}

\item{max_dist}{Numeric; maximum distance from a cluster's seed. Must be finite and
non-negative.}

\item{weights}{Optional numeric vector, one length per edge; \code{NULL} makes
\code{max_dist} a hop count.}

\item{seeds}{Optional integer vector of 0-based nodes to try as seeds, in order of
preference. Any node left unassigned afterwards becomes a seed in ascending index
order; \code{NULL} seeds in ascending index order throughout.}
}
\value{
List with \code{labels} (integer cluster index per node, contiguous in
\verb{[0, n_clusters)} and numbered in the order the clusters were grown; every node is
labelled) and \code{n_clusters} (integer).
}
\description{
Repeatedly takes an unassigned node as a seed and grows a cluster outwards from it,
absorbing any node reachable within \code{max_dist} that no earlier cluster has already
claimed. Collapsing each cluster to its centroid gives a coarser graph whose nodes
are spaced by roughly \code{max_dist}, which is what makes this useful as mesh or
skeleton downsampling.
}
\details{
The radius is the \strong{true geodesic distance from the seed}, not the length of the
walk that happened to reach it — so a node close to a seed is never excluded merely
because a traversal arrived at it the long way round.
}
