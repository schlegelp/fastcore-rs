% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{unique_edges}
\alias{unique_edges}
\title{Unique undirected edges of a triangle mesh.}
\usage{
unique_edges(faces, vertices = NULL, threads = NULL)
}
\arguments{
\item{faces}{Integer or numeric \verb{(F, 3)} matrix of triangle vertex indices
(0-based).}

\item{vertices}{Optional numeric \verb{(V, 3)} matrix of vertex coordinates. If given,
also returns the euclidean length of each unique edge.}

\item{threads}{Optional integer; number of threads. \code{NULL} uses all cores.}
}
\value{
List with \code{edges} (integer \verb{(n_unique, 2)} matrix of \verb{[min, max]} rows,
sorted ascending by \verb{(max, min)}) and \code{lengths} (numeric edge lengths, or \code{NULL}
when \code{vertices} was \code{NULL}).
}
\description{
Each face \verb{(a, b, c)} contributes the edges \verb{(a, b)}, \verb{(b, c)}, \verb{(c, a)}. Edges are
undirected, so each pair is normalised to \verb{[min, max]} before dedup; self-loops
from degenerate faces are kept.
}
