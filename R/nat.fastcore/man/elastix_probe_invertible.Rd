% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/elastix.R
\name{elastix_probe_invertible}
\alias{elastix_probe_invertible}
\title{Can an Elastix transform be inverted?}
\usage{
elastix_probe_invertible(path)
}
\arguments{
\item{path}{Path to a \code{TransformParameters.*.txt} file. Its initial-transform chain is
followed, however deep.}
}
\value{
\code{TRUE} if \code{\link[=elastix_xform_inv]{elastix_xform_inv()}} would work on it.

Errors if the file would not load at all (missing, not an Elastix transform, an unsupported
transform kind, binary parameters, a circular chain) -- so \code{TRUE} is a promise, not a guess.
}
\description{
Answered \strong{without reading the transform's coefficients}, and so cheaply enough to ask about
many files at once.
}
\details{
A transform cannot be inverted exactly when some step in its chain combines via \code{Add}:
\code{T(x) = T_initial(x) + T_this(x) - x} does not decompose into invertible hops. That fact lives
in one short key -- but the key sits \emph{after} a coefficient array that runs to tens of megabytes,
so answering it honestly used to mean parsing the whole file.

This walks the same chain and applies the same validation, skipping only the numbers: roughly
\strong{20x} faster than \code{\link[=elastix_read]{elastix_read()}} across real registrations, and ~200x on the largest.

Use it when you must label \emph{many} files up front and cannot afford to parse them -- building a
bridging graph, say, where each edge needs to know whether it can be walked backwards.
}
\examples{
\dontrun{
elastix_probe_invertible("TransformParameters.FixedFANC.txt")
}

}
\seealso{
\code{\link[=elastix_read]{elastix_read()}}, \code{\link[=elastix_xform_inv]{elastix_xform_inv()}}
}
