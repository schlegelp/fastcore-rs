% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{geodesic_nearest_mesh}
\alias{geodesic_nearest_mesh}
\title{Distance to the nearest target vertex, for each source vertex of a mesh.}
\usage{
geodesic_nearest_mesh(
  faces,
  n_vertices,
  vertices,
  sources,
  targets,
  limit,
  threads
)
}
\arguments{
\item{faces}{Integer or numeric \verb{(F, 3)} matrix of triangle vertex indices.}

\item{n_vertices}{Integer; total number of vertices in the mesh.}

\item{vertices}{Optional numeric \verb{(V, 3)} matrix of vertex coordinates; \code{NULL}
counts edges.}

\item{sources}{Optional integer vector of source vertex indices; \code{NULL} uses
every vertex.}

\item{targets}{Optional integer vector of target vertex indices; \code{NULL} uses
every vertex.}

\item{limit}{Optional numeric; ignore targets further away than this.}

\item{threads}{Optional integer; number of threads. \code{NULL} uses all cores.}
}
\value{
A list with \code{distances} and \code{nearest}.
}
\description{
A memory-efficient alternative to \code{geodesic_matrix_mesh}: it keeps only the
nearest target and the distance to it, so the output is \code{O(sources)} rather than
\code{O(sources * targets)}. It is also faster, because the search stops at the first
target it settles rather than exploring the whole component.
}
\details{
Returns a list with \code{distances} and \code{nearest} (the vertex index of that nearest
target). Sources with no reachable target get \code{-1}. A source that is itself a
target is matched to its nearest \emph{other} target, never to itself.
}
