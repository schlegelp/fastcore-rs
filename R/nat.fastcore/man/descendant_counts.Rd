% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{descendant_counts}
\alias{descendant_counts}
\title{How many nodes lie strictly below each node.}
\usage{
descendant_counts(parents, targets = NULL)
}
\arguments{
\item{parents}{Integer vector of 0-based parent indices (roots are \verb{< 0}).}

\item{targets}{Optional integer vector of 0-based node indices; \code{NULL} counts
every node, otherwise only these are counted.}
}
\value{
Numeric vector of counts, aligned with \code{parents}. A node is never its own
descendant, so a leaf scores \code{0} even when it is itself a target. Double rather
than integer for the same reason as \code{betweenness}.
}
\description{
This is what \code{navis::betweeness_centrality(from_ = ...)} actually computes, and
what \code{navis::find_main_branchpoint(method = "betweenness")} — its one caller —
wants.
}
