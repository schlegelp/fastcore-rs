% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{descendants}
\alias{descendants}
\title{Every node in the sub-tree below each source.}
\usage{
descendants(parents, sources)
}
\arguments{
\item{parents}{Integer vector of 0-based parent indices (roots are \verb{< 0}).}

\item{sources}{Integer vector of 0-based node indices to walk down from.}
}
\value{
List of integer vectors, one per source in \code{sources} order. Each starts
with the source itself and is in depth-first pre-order, so a node always
precedes its own descendants. An out-of-range source gives an empty vector.
}
\description{
The replacement for \code{igraph::subcomponent(mode = "in")}, and what makes "cut the
skeleton here" a masking operation rather than a graph rebuild.
}
