% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mesh.R
\name{mesh_connected_components}
\alias{mesh_connected_components}
\title{Connected components of a triangle mesh}
\usage{
mesh_connected_components(
  faces,
  n_vertices = NULL,
  connectivity = c("vertex", "face", "manifold"),
  threads = NULL
)
}
\arguments{
\item{faces}{Integer or numeric \verb{(F, 3)} matrix of triangle vertex indices (0-based).}

\item{n_vertices}{Integer; total number of vertices in the mesh. Belongs to
\code{connectivity = "vertex"}, where it is required: it sets the length of the result,
and vertices in no face are components of size one.}

\item{connectivity}{One of \code{"vertex"} (default), \code{"face"} or \code{"manifold"}; what two
faces must share to count as connected - a corner, any edge, or an edge carrying
exactly two faces.}

\item{threads}{Integer cap on the thread count for this call, or \code{NULL} for the
process-wide pool. Belongs to \code{connectivity = "face"} and \code{"manifold"}.}
}
\value{
Integer vector: one entry per vertex giving the smallest vertex index in its
component (\code{"vertex"}), or one entry per face giving the smallest face index in its
component (\code{"face"} and \code{"manifold"}).
}
\description{
Three readings of "connected", chosen with \code{connectivity}, each strictly finer than
the one before it:
\describe{
\item{\code{"vertex"} (the default)}{Joins two vertices whenever a face names them
both, so a face joins its three corners. One label per \emph{vertex}.}
\item{\code{"face"}}{Joins two faces wherever they share an \strong{edge}, not merely a
corner. One label per \emph{face}.}
\item{\code{"manifold"}}{Joins two faces only across an edge carrying \strong{exactly two}
of them. Likewise one label per \emph{face}.}
}
}
\details{
Each step drops a kind of junction. Going from \code{"vertex"} to \code{"face"} drops the pinch
points: two triangles meeting at a single corner are one component under the first and
two under the second, because there is no edge to step across. Going from \code{"face"} to
\code{"manifold"} drops the seams: three sheets meeting along one edge are one component
under \code{"face"} and three under \code{"manifold"}, because an edge that deep belongs to no
single surface.

So pick by what the components are \emph{for}. \code{"vertex"} answers "can these vertices reach
each other along mesh edges" - the question \code{\link[=geodesic_matrix_mesh]{geodesic_matrix_mesh()}} answers with
distances. \code{"face"} splits a mesh into the pieces you could walk across. \code{"manifold"}
splits it into pieces that are \emph{surfaces}, each with a well-defined inside, which is
what you want before asking a piece for its volume or its winding; it reproduces
\code{trimesh}'s \code{face_adjacency} exactly. There is no per-vertex form of the face answers,
and that is not a gap in the interface: a pinch vertex belongs to several face
components at once.

All three are Union-Find with path-halving. \code{"vertex"} builds no adjacency at all, one
serial sweep over a single integer vector; the other two group the \verb{3F} edges the faces
name first, which is a parallel sort and the only reason they take \code{threads}. They then
differ by one test on how many faces each edge came back with, so \code{"manifold"} costs no
more than \code{"face"}.

An edge with a single face on it - the boundary of an open mesh - joins nothing under
any reading; there is no second face to join it to. Self-loop edges from degenerate
faces are kept throughout, as in \code{\link[=unique_edges]{unique_edges()}}.
}
\examples{
# Two triangles pinched together at vertex 2 and joined nowhere else.
faces <- matrix(c(0, 1, 2, 2, 3, 4), ncol = 3, byrow = TRUE)

# The vertex graph walks straight through the pinch...
mesh_connected_components(faces, n_vertices = 5)

# ...the faces cannot step across it.
mesh_connected_components(faces, connectivity = "face")

# Three fins meeting along the spine (1, 2): one component across any shared edge,
# three once the spine is too deep to belong to a single surface.
fins <- matrix(c(1, 2, 3, 1, 2, 4, 1, 2, 5), ncol = 3, byrow = TRUE)
mesh_connected_components(fins, connectivity = "face")
mesh_connected_components(fins, connectivity = "manifold")
}
\seealso{
\code{\link[=connected_components_graph]{connected_components_graph()}}, the same question from an edge list.
}
