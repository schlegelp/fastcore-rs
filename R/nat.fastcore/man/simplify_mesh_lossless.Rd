% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{simplify_mesh_lossless}
\alias{simplify_mesh_lossless}
\title{Simplify a triangle mesh without changing its shape.}
\usage{
simplify_mesh_lossless(
  faces,
  vertices,
  epsilon = 0.001,
  max_iterations = 9999,
  preserve_border = FALSE,
  lock = NULL
)
}
\arguments{
\item{faces}{Integer or numeric \verb{(F, 3)} matrix of triangle vertex indices
(0-based).}

\item{vertices}{Numeric \verb{(V, 3)} matrix of vertex coordinates. Must be finite.}

\item{epsilon}{Numeric quadric error below which an edge may collapse. An
\emph{absolute} error with units of squared distance, so it scales with your
coordinates. Default 1e-3.}

\item{max_iterations}{Integer cap on the number of passes. Default 9999.}

\item{preserve_border}{Logical; freeze every vertex on a mesh boundary.}

\item{lock}{Optional logical vector, one entry per vertex; as \code{\link[=simplify_mesh]{simplify_mesh()}}.}
}
\value{
List with \code{vertices}, \code{faces} and \code{vertex_map}, as \code{\link[=simplify_mesh]{simplify_mesh()}},
including when an entry of \code{vertex_map} is \code{-1}. The "whole piece consumed"
case arrives by a different route here: there is no face budget, but \code{epsilon}
is an \emph{absolute} error, so a component small enough that all of its edges fall
under it collapses away entirely.
}
\description{
Collapses only edges whose quadric error is below \code{epsilon} and repeats until a
whole pass changes nothing. There is no face budget: this sheds over-tessellation
\itemize{
\item coplanar fans, duplicate vertices, degenerate faces - rather than hitting a
target. Use \code{\link[=simplify_mesh]{simplify_mesh()}} for that.
}
}
\details{
Note that "lossless" is a claim about the surface, not the outline: a quadric
measures distance to the planes of the incident faces, and the plane of a flat
patch says nothing about where that patch ends. Pass \code{preserve_border = TRUE} on
open meshes.
}
