% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clustering.R
\name{nblast_hclust}
\alias{nblast_hclust}
\title{Hierarchical clustering of a score matrix}
\usage{
nblast_hclust(
  scores,
  method = c("ward", "single", "complete", "average", "weighted", "centroid", "median"),
  symmetry = c("mean", "min", "max", "none"),
  transform = c("one_minus", "none"),
  labels = NULL,
  n_cores = NULL
)
}
\arguments{
\item{scores}{Square \verb{(n, n)} numeric score matrix. The diagonal is never read,
so self-scores may be left on it.}

\item{method}{Linkage method: one of \code{"ward"}, \code{"single"}, \code{"complete"},
\code{"average"}, \code{"weighted"}, \code{"centroid"} or \code{"median"}. Names follow SciPy, so
\code{"ward"} is R's \code{"ward.D2"} and \code{"weighted"} is R's \code{"mcquitty"}. Note that
\code{"centroid"} and \code{"median"} take \strong{plain} distances here, whereas
\code{\link[stats:hclust]{stats::hclust()}} expects squared ones for those two; the heights returned
here equal \code{sqrt()} of what \code{hclust(d^2, ...)} gives.}

\item{symmetry}{How to combine cell \verb{(i, j)} with \verb{(j, i)}, since NBLAST is not
symmetric: \code{"mean"} (the default), \code{"min"}, \code{"max"}, or \code{"none"} to take the
upper triangle as given. Matches the \code{symmetry} argument of
\code{\link[=nblast_allbyall]{nblast_allbyall()}}; use \code{"none"} if the matrix is already symmetric, which is
also the fastest path.}

\item{transform}{\code{"one_minus"} (the default) turns a similarity into a distance
via \code{1 - score}; \code{"none"} treats the values as distances already.}

\item{labels}{Optional character vector of \code{n} names for the observations, or
\code{NULL}. Falls back to the matrix's row names.}

\item{n_cores}{Optional integer thread count for the condensing pass; \code{NULL}
uses all cores.}
}
\value{
An object of class \code{hclust}, so \code{\link[stats:cutree]{stats::cutree()}}, \code{\link[=plot]{plot()}} and
\code{\link[stats:dendrogram]{stats::as.dendrogram()}} all work on it directly.
}
\description{
Clusters a square score matrix -- typically NBLAST output from
\code{\link[=nblast_allbyall]{nblast_allbyall()}} -- and returns a standard \code{\link[stats:hclust]{stats::hclust()}} object.
}
\details{
Unlike \code{\link[stats:hclust]{stats::hclust()}}, which refuses more than 65536 observations, this has
no size ceiling beyond available memory. That limit is a hard blocker for
whole-brain connectome work, where 100k-200k neurons is routine.

Symmetrising, the score-to-distance transform and condensing to the upper
triangle are \strong{fused into a single pass}, and the resulting buffer is then
clustered in place. The idiomatic R spelling,
\code{hclust(as.dist(1 - (m + t(m)) / 2))}, materialises three further \verb{n x n}
matrices before clustering even begins, which at these sizes is where the
memory goes.
}
\examples{
# A score matrix with two obvious pairs.
m <- matrix(c(
  1.00, 0.90, 0.20, 0.10,
  0.70, 1.00, 0.30, 0.15,
  0.25, 0.35, 1.00, 0.80,
  0.05, 0.10, 0.60, 1.00
), 4, 4, byrow = TRUE)

h <- nblast_hclust(m, method = "average")
cutree(h, 2)

}
\seealso{
\code{\link[=nblast_dist]{nblast_dist()}} for the condensed distances on their own,
\code{\link[=fast_hclust]{fast_hclust()}} to cluster an existing \code{dist}.
}
