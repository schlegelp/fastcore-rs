% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{resample_skeleton}
\alias{resample_skeleton}
\title{Place nodes at a fixed spacing along every neurite.}
\usage{
resample_skeleton(parents, x, y, z, spacing, threads = NULL)
}
\arguments{
\item{parents}{Integer vector of 0-based parent indices (roots are \verb{< 0}).}

\item{x, y, z}{Numeric vectors of node coordinates, one entry per node.}

\item{spacing}{Numeric; target distance between adjacent nodes.}

\item{threads}{Optional integer; number of threads. \code{NULL} uses all cores.}
}
\value{
List with \code{parents} (0-based parent index per output node, \code{-1} for roots),
\code{x}, \code{y}, \code{z} (their coordinates), \code{source_from} and \code{source_to} (the 0-based
\emph{input} node indices of the edge each output node sits on, child then parent) and
\code{alpha} (how far along that edge it lies, from the child end). The input's roots,
branch points and leafs come first, in input order and unmoved; they carry their own
index in both \code{source_} columns and an \code{alpha} of 0, so
\code{attr[source_from + 1] * (1 - alpha) + attr[source_to + 1] * alpha} interpolates any
per-node quantity over the whole output.
}
\description{
The inverse problem to \code{downsample_skeleton()}: rather than thinning what is there,
this re-samples each segment from scratch, so a skeleton whose node density varies
tenfold between neurites comes out evenly sampled throughout. Each segment is divided
into \code{round(length / spacing)} equal parts (at least one), so both of its endpoints
land exactly and no runt edge is left over; a segment shorter than \code{spacing / 2}
collapses to a single straight edge.
}
