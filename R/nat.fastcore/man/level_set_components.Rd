% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{level_set_components}
\alias{level_set_components}
\title{Connected components of every level set at once.}
\usage{
level_set_components(edges, n_nodes, labels)
}
\arguments{
\item{edges}{Integer or numeric \verb{(E, 2)} matrix of edges (0-based node indices).}

\item{n_nodes}{Integer; total number of nodes.}

\item{labels}{Integer vector with one label per node. \strong{Negative labels mark
excluded nodes}: they join no component and come back as \code{-1}. That is what lets
you feed in the output of a search that could not reach everything —
\verb{geodesic_matrix_*} returns \code{-1} for unreachable — rather than lumping every
unreachable node into one bogus level.}
}
\value{
List with \code{ids} (integer component index per node in \verb{[0, n_components)},
or \code{-1} for excluded nodes; assigned in order of first appearance scanning nodes
low to high) and \code{n_components} (integer).
}
\description{
Given a label per node, finds the connected components of each subgraph induced by
the nodes sharing a label — all labels in a single pass, by unioning an edge only
when its two endpoints agree. This is the inner loop of wavefront-style mesh
skeletonization, where the label is a binned geodesic distance and each component
is one ring around the structure.
}
\details{
Done conventionally that loop costs one subgraph construction plus one component
search per distinct label; here it is one sweep over the edges.
}
