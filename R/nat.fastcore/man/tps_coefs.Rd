% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/warp.R
\name{tps_coefs}
\alias{tps_coefs}
\title{Coefficients of a fitted thin-plate spline}
\usage{
tps_coefs(reg)
}
\arguments{
\item{reg}{A \code{tps_transform}, from \code{\link[=tps_transform]{tps_transform()}}.}
}
\value{
A list with elements \code{source} (\verb{(M, 3)}), \code{W} (\verb{(M, 3)}) and \code{A} (\verb{(4, 3)}, whose
first row is the translation).
}
\description{
Returns the fit itself: \code{W}, the weights of the non-affine part, and \code{A}, the
coefficients of the affine part. Together with the source landmarks these fully determine
the transform, so they are what you serialise if you want to store a fit rather than
repeat it.
}
\examples{
src <- rbind(c(0, 0, 0), c(10, 10, 10), c(100, 100, 100), c(80, 10, 30))
trg <- rbind(c(1, 15, 5), c(9, 18, 21), c(80, 99, 120), c(5, 10, 80))
co <- tps_coefs(tps_transform(src, trg))
dim(co$W)
dim(co$A)

}
\seealso{
\code{\link[=tps_transform]{tps_transform()}}
}
