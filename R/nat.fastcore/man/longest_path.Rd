% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{longest_path}
\alias{longest_path}
\title{The longest path from any node to its root.}
\usage{
longest_path(parents, weights = NULL)
}
\arguments{
\item{parents}{Integer vector of 0-based parent indices (roots are \verb{< 0}).}

\item{weights}{Optional numeric vector of child-to-parent edge weights; \code{NULL}
counts edges.}
}
\value{
Integer vector of 0-based node indices along the path, \strong{distal first} —
so the first element is the far end and the last is a root. Ties break towards
the lowest node index.
}
\description{
Not the NP-hard general problem: in a rooted forest every maximal path is fixed by
its start node, so this is a distances-to-root question.
}
