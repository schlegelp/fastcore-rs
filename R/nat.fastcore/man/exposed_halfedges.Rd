% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{exposed_halfedges}
\alias{exposed_halfedges}
\title{The edges a subset is about to expose. Takes the faces \emph{before} subsetting.}
\usage{
exposed_halfedges(faces, dropped, threads = NULL)
}
\arguments{
\item{faces}{Integer or numeric \verb{(F, 3)} matrix of faces \emph{before} subsetting
(0-based vertex indices).}

\item{dropped}{Logical vector of length \code{V}: for each vertex, whether the subset
drops it.}

\item{threads}{Optional integer; number of threads. \code{NULL} uses all cores.}
}
\value{
Integer \verb{(K, 2)} matrix of directed half-edges, with indices into the
\emph{original} vertices. Remap them onto the surviving vertices before capping.
}
\description{
A face survives only if all three of its corners do, so an edge ends up on a new
boundary exactly when it loses a face to the cut but keeps one. Both halves of that
test are local to the cut, so — unlike \code{boundary_halfedges} — this never groups the
edges of the whole mesh.
}
\details{
Edges that were boundary \emph{already} are left out: they belong to openings the mesh
came with, and sealing those is \code{boundary_halfedges}' job. One consequence is worth
knowing: where a cut runs into an opening the mesh came with, what it exposes is an
open chain rather than a ring, and \code{trace_loops} abandons it.
}
