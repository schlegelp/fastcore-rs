% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{simplify_mesh}
\alias{simplify_mesh}
\title{Simplify a triangle mesh, tracking where every vertex went.}
\usage{
simplify_mesh(
  faces,
  vertices,
  ratio = NULL,
  n_faces = NULL,
  aggressiveness = 7,
  preserve_border = FALSE,
  lock = NULL
)
}
\arguments{
\item{faces}{Integer or numeric \verb{(F, 3)} matrix of triangle vertex indices
(0-based).}

\item{vertices}{Numeric \verb{(V, 3)} matrix of vertex coordinates. Must be finite.}

\item{ratio}{Numeric fraction of the faces to keep, in \verb{(0, 1]}.}

\item{n_faces}{Integer number of faces to keep. Give exactly one of \code{ratio} or
\code{n_faces}.}

\item{aggressiveness}{Numeric exponent of the error-threshold sweep. Higher
reaches the target in fewer, coarser passes. Default 7.}

\item{preserve_border}{Logical; freeze every vertex on a mesh boundary.}

\item{lock}{Optional logical vector, one entry per vertex. A locked vertex is
never merged into another and never moved, so it keeps its exact coordinates.}
}
\value{
List with \code{vertices} (numeric \verb{(V', 3)} matrix), \code{faces} (integer
\verb{(F', 3)} matrix, 0-based) and \code{vertex_map} (integer, one entry per \emph{original}
vertex giving its 0-based index in \code{vertices}, or \code{-1} if it did not survive).

Being \emph{merged} is not a \code{-1}: a collapsed vertex carries the index of whatever
it merged into, which is the point of the map. An entry is \code{-1} exactly when
the vertex it ended up in is referenced by no surviving face, which takes one
of four forms: it was in no face to begin with; it was only ever in zero-area
faces, which are dropped on the way in and so reduce to the first case; the
whole piece it belonged to was consumed, since nothing is reserved per
connected component and a small fragment goes once the target is tight enough;
or the input was degenerate throughout and the output mesh is empty. Mask with
\code{vertex_map >= 0} before aggregating.
}
\description{
Iteratively contracts the edge whose collapse costs least under the
Garland-Heckbert quadric error. Unlike other implementations of this algorithm it
also reports, for every vertex of the original mesh, which vertex of the
simplified mesh it ended up in - so per-vertex data survives the simplification.
}
\details{
Non-manifold input is fine: no manifoldness is assumed or checked, and each
collapse guard skips what it cannot handle rather than failing.
}
