% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{trace_loops}
\alias{trace_loops}
\title{Walk directed half-edges into closed rings.}
\usage{
trace_loops(halfedges)
}
\arguments{
\item{halfedges}{Integer or numeric \verb{(K, 2)} matrix of directed half-edges, as
returned by \code{boundary_halfedges} or \code{exposed_halfedges}.}
}
\value{
List with \code{rings} (integer vector: every ring's vertices, end to end) and
\code{offsets} (integer vector, one longer than there are rings). Ring \code{i} is
\code{rings[(offsets[i] + 1):offsets[i + 1]]} — the offsets are 0-based bounds, as
the vertex indices are.
}
\description{
Greedy: at a non-manifold boundary vertex several half-edges leave at once, so this
takes whichever is still free. Every half-edge lands in exactly one ring, which is
what makes this cover the whole boundary — a cycle basis quietly drops the edges
that are not part of a simple cycle, and those holes stay open.
}
\details{
A walk that runs into a dead end is abandoned, and so is a ring of fewer than three
vertices, so the rings need not account for every half-edge handed in.
}
