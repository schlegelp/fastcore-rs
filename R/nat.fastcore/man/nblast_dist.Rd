% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clustering.R
\name{nblast_dist}
\alias{nblast_dist}
\title{Condensed distances from a score matrix}
\usage{
nblast_dist(
  scores,
  symmetry = c("mean", "min", "max", "none"),
  transform = c("one_minus", "none"),
  labels = NULL,
  n_cores = NULL
)
}
\arguments{
\item{scores}{Square \verb{(n, n)} numeric score matrix. The diagonal is not read.}

\item{symmetry}{How to combine cell \verb{(i, j)} with \verb{(j, i)}: \code{"mean"} (default),
\code{"min"}, \code{"max"}, or \code{"none"}.}

\item{transform}{\code{"one_minus"} (default) for \code{1 - score}, or \code{"none"} if the
values are already distances.}

\item{labels}{Optional character vector of \code{n} names, or \code{NULL}. Falls back to
the matrix's row names.}

\item{n_cores}{Optional integer thread count; \code{NULL} uses all cores.}
}
\value{
An object of class \code{dist} with \code{n * (n - 1) / 2} entries.
}
\description{
Symmetrises a score matrix, converts it to distances and condenses it to the
upper triangle in a single fused pass, returning a \code{\link[stats:dist]{stats::dist()}} object. The
only allocation is the result itself, where \code{as.dist(1 - (m + t(m)) / 2)} builds
several full \verb{n x n} copies on the way.
}
\details{
R stores a \code{dist} as the lower triangle by column, which for a symmetric matrix
is element-for-element the same sequence as the upper triangle by row, so no
rearranging is needed.
}
\examples{
m <- matrix(c(1, 0.75, 0.25, 1), 2, 2)
nblast_dist(m)

}
\seealso{
\code{\link[=nblast_hclust]{nblast_hclust()}} to go straight to a dendrogram without materialising
the distances at all.
}
