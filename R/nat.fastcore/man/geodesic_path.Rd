% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{geodesic_path}
\alias{geodesic_path}
\title{The shortest route from one source to each target, as node sequences.}
\usage{
geodesic_path(
  edges,
  n_nodes,
  source,
  targets,
  weights = NULL,
  directed = FALSE,
  precision = 32
)
}
\arguments{
\item{edges}{Integer or numeric \verb{(E, 2)} matrix of edges (0-based node indices).}

\item{n_nodes}{Integer; total number of nodes.}

\item{source}{Integer; the 0-based node to start from.}

\item{targets}{Integer vector of 0-based nodes to reach.}

\item{weights}{Optional numeric vector, one length per edge; \code{NULL} counts edges.}

\item{directed}{Logical; if \code{TRUE} an edge \verb{(u, v)} may only be traversed from \code{u}
to \code{v}.}

\item{precision}{Integer; 32 or 64, the width the search accumulates at. Nothing
here returns a distance, so this changes only which answer close ties resolve to.}
}
\value{
List of integer vectors, one per target in \code{targets} order, each running
source-first to target-last. An unreachable target gives an empty vector.
}
\description{
The shortest route from one source to each target, as node sequences.
}
