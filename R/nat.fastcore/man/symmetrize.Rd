% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clustering.R
\name{symmetrize}
\alias{symmetrize}
\title{Symmetrise a score matrix}
\usage{
symmetrize(scores, symmetry = c("mean", "min", "max", "none"), n_cores = NULL)
}
\arguments{
\item{scores}{Square \verb{(n, n)} numeric score matrix. The diagonal is not read, so
self-scores may be left on it.}

\item{symmetry}{How to combine \verb{(i, j)} with \verb{(j, i)}: \code{"mean"} (the default),
\code{"min"}, \code{"max"}, or \code{"none"} to mirror the upper triangle onto the lower
instead of combining.}

\item{n_cores}{Optional integer thread count; \code{NULL} uses all cores.}
}
\value{
A symmetric \verb{(n, n)} matrix, with \code{dimnames} carried over. \code{scores}
itself is unchanged -- R's value semantics forbid writing to it, which is the
one thing this cannot do that the Python version can.
}
\description{
Combines cell \verb{(i, j)} with \verb{(j, i)}, since NBLAST is not symmetric. The
idiomatic \code{(m + t(m)) / 2} builds two further \verb{n x n} matrices on the way to the
answer; this builds one, the result.
}
\details{
You do not need this before \code{\link[=nblast_hclust]{nblast_hclust()}} or \code{\link[=nblast_dist]{nblast_dist()}} -- both take a
\code{symmetry} argument and fold the same combine into their own fused pass. Reach
for it when something \emph{else} has to read the matrix: plotting it, writing it out,
or handing it to a function that assumes symmetry.
}
\examples{
m <- matrix(c(1, 0.4, 0.8, 1), 2, 2)
symmetrize(m)

}
\seealso{
\code{\link[=nblast_dist]{nblast_dist()}} and \code{\link[=nblast_hclust]{nblast_hclust()}}, which symmetrise as part of a
single pass over the matrix.
}
