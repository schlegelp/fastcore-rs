% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{reroot}
\alias{reroot}
\title{Re-orient a forest so that each of \code{new_roots} becomes its component's root.}
\usage{
reroot(parents, new_roots)
}
\arguments{
\item{parents}{Integer vector of 0-based parent indices (roots are \verb{< 0}).}

\item{new_roots}{Integer vector of 0-based node indices to root at. Two roots in
the same component: the last one named wins.}
}
\value{
Integer vector of new 0-based parent indices, aligned with \code{parents}.
Roots are \code{-1}.
}
\description{
Only the edges between each new root and its component's old root are reversed;
components nobody names come back byte-identical. The general form of
\code{reroot_rewire}, which takes one preferred root plus a set of new edges.
}
