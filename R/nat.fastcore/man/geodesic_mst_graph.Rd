% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{geodesic_mst_graph}
\alias{geodesic_mst_graph}
\title{Minimum spanning tree over a subset of graph nodes, by geodesic distance.}
\usage{
geodesic_mst_graph(
  edges,
  n_nodes,
  nodes,
  weights = NULL,
  limit = NULL,
  threads = NULL,
  precision = 32
)
}
\arguments{
\item{edges}{Integer or numeric \verb{(E, 2)} matrix of edges (0-based node indices).}

\item{n_nodes}{Integer; total number of nodes.}

\item{nodes}{Integer vector of 0-based nodes to span. Must be distinct.}

\item{weights}{Optional numeric vector, one length per edge; \code{NULL} counts edges.}

\item{limit}{Optional numeric; do not join nodes further apart than this.}

\item{threads}{Optional integer; number of threads. \code{NULL} uses all cores.}

\item{precision}{Integer; 32 or 64, the width distances are accumulated at. The
result is numeric either way — R has no float32 — so this buys accuracy.}
}
\value{
List with \code{edges} (integer \verb{(M, 2)} matrix of 0-based positions in \code{nodes},
ascending by weight) and \code{weights} (numeric geodesic distance across each).
}
\description{
The edge-list form of \code{geodesic_mst_mesh}, which explains why this never builds the
\verb{k x k} distance matrix the question seems to call for. Always undirected — a
minimum spanning tree of a directed graph is a different problem (an arborescence)
with a different algorithm.
}
