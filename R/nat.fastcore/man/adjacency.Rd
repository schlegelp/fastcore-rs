% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{adjacency}
\alias{adjacency}
\title{The skeleton's adjacency matrix, as the three arrays of a CSR matrix.}
\usage{
adjacency(parents, weights = NULL, directed = TRUE, transpose = FALSE)
}
\arguments{
\item{parents}{Integer vector of 0-based parent indices (roots are \verb{< 0}).}

\item{weights}{Optional numeric vector of child-to-parent edge weights; \code{NULL}
gives every edge weight 1.}

\item{directed}{Logical; if \code{TRUE} (default) only child-to-parent edges are
stored, if \code{FALSE} both directions are.}

\item{transpose}{Logical; if \code{TRUE} store parent-to-child instead. Ignored when
\code{directed} is \code{FALSE}, where the matrix is symmetric anyway.}
}
\value{
List with \code{indptr} (integer, \code{N + 1} entries), \code{indices} (integer column
index per non-zero) and \code{data} (numeric weight per non-zero). Rows and columns
are in node-index order and column indices ascend within each row.
}
\description{
Handing back the raw arrays rather than a matrix object keeps this package free of
a sparse-matrix dependency; feed them to \code{Matrix::sparseMatrix(p = indptr, i = indices, x = data, ...)} if you want one.
}
