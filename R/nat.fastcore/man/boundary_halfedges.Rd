% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{boundary_halfedges}
\alias{boundary_halfedges}
\title{Every edge of a mesh that has only one face on it.}
\usage{
boundary_halfedges(faces, threads = NULL)
}
\arguments{
\item{faces}{Integer or numeric \verb{(F, 3)} matrix of triangle vertex indices
(0-based).}

\item{threads}{Optional integer; number of threads. \code{NULL} uses all cores.}
}
\value{
Integer \verb{(K, 2)} matrix of directed half-edges, in the order they appear
in the \verb{3F} edge list.
}
\description{
An interior edge has two faces on it and a boundary edge has one, so this is a
grouping of the \verb{3F} edges the faces name — and that grouping is the whole cost.
Use \code{exposed_halfedges} instead where you already know which vertices are going
away: that one never looks at the mesh as a whole.
}
\details{
The half-edges come back \strong{directed}, wound the way the one face they have left
winds them. That is what makes \code{trace_loops} a walk rather than a search, and what
tells \code{triangulate_rings} which way round to wind the cap.
}
