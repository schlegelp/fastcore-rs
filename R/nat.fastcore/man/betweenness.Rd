% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{betweenness}
\alias{betweenness}
\title{Betweenness centrality, in \code{O(N)} rather than Brandes' \code{O(V*E)}.}
\usage{
betweenness(parents, directed = TRUE)
}
\arguments{
\item{parents}{Integer vector of 0-based parent indices (roots are \verb{< 0}).}

\item{directed}{Logical; if \code{TRUE} only count paths running child-to-parent.}
}
\value{
Numeric vector of path counts, aligned with \code{parents}. Returned as double
rather than integer because an undirected 100k-node skeleton reaches ~5e9, well
past R's 32-bit integer.
}
\description{
Shortest paths in a tree are unique, so the count through a node is a closed form:
descendants times ancestors when directed, and a sum of products over the parts it
separates when not. Pairs are only counted within a connected component.
}
\details{
Note this is \emph{not} \code{navis::betweeness_centrality(from_ = ...)}, which despite the
name computes a descendant count — see \code{descendant_counts}.
}
