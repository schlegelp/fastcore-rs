% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{simplify_rdp}
\alias{simplify_rdp}
\title{Drop the nodes that do not bend a neurite, by Ramer-Douglas-Peucker.}
\usage{
simplify_rdp(
  parents,
  x,
  y,
  z,
  epsilon,
  preserve = NULL,
  weights = NULL,
  threads = NULL
)
}
\arguments{
\item{parents}{Integer vector of 0-based parent indices (roots are \verb{< 0}).}

\item{x, y, z}{Numeric vectors of node coordinates, one entry per node.}

\item{epsilon}{Numeric; how far the simplified path may stray from the original, in
the units of the coordinates. \code{0} still drops nodes that are \emph{exactly} collinear,
and nothing else.}

\item{preserve}{Optional logical vector, one entry per node, marking extra nodes that
must survive; \code{NULL} for none.}

\item{weights}{Optional numeric vector of child-to-parent edge weights; \code{NULL}
returns no \code{weights}.}

\item{threads}{Optional integer; number of threads. \code{NULL} uses all cores.}
}
\value{
List with \code{nodes}, \code{parents} and \code{weights}, as \code{downsample_skeleton()}.
}
\description{
Where \code{downsample_skeleton()} thins by counting, this thins by \emph{shape}: a node
survives only if removing it would move the traced path by more than \code{epsilon}. Long
straight stretches collapse to their two ends while a tight curve keeps every node it
needs, so the same tolerance buys a much better skeleton per node than a fixed factor
does.
}
\details{
Each replacement edge carries the length of the chain it stands in for, so geodesic
distances stay right even where the geometry has been cut across.
}
