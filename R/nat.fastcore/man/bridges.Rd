% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{bridges}
\alias{bridges}
\title{Which edges are bridges — the ones whose removal would disconnect their component.}
\usage{
bridges(edges, n_nodes)
}
\arguments{
\item{edges}{Integer or numeric \verb{(E, 2)} matrix of edges (0-based node indices).
Direction is ignored.}

\item{n_nodes}{Integer; total number of nodes.}
}
\value{
Logical vector with one flag per input edge, \code{TRUE} for a bridge.
}
\description{
Tarjan's algorithm, one depth-first sweep. The counterpart to
\code{minimum_spanning_tree} rather than a variant of it: the MST asks which edges to
\emph{keep} to stay connected, this asks which ones may not be \emph{dropped}. That is the
question behind "prune this graph but do not shatter it".
}
\details{
Parallel edges are honoured — two nodes joined twice are joined by a cycle, so
neither of those edges is a bridge. Self-loops are never bridges.
}
