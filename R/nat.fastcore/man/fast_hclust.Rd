% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clustering.R
\name{fast_hclust}
\alias{fast_hclust}
\title{Hierarchical clustering without the 65536 observation limit}
\usage{
fast_hclust(
  d,
  method = c("ward", "single", "complete", "average", "weighted", "centroid", "median"),
  labels = NULL
)
}
\arguments{
\item{d}{A \code{dist} object, or a bare numeric vector of length \code{n * (n - 1) / 2}
holding the lower triangle by column.}

\item{method}{Linkage method: one of \code{"ward"}, \code{"single"}, \code{"complete"},
\code{"average"}, \code{"weighted"}, \code{"centroid"} or \code{"median"}. Names follow SciPy, so
\code{"ward"} is R's \code{"ward.D2"} and \code{"weighted"} is R's \code{"mcquitty"}. Note that
\code{"centroid"} and \code{"median"} take \strong{plain} distances here, whereas
\code{\link[stats:hclust]{stats::hclust()}} expects squared ones for those two; the heights returned
here equal \code{sqrt()} of what \code{hclust(d^2, ...)} gives.}

\item{labels}{Optional character vector of \code{n} names, or \code{NULL} to use the
\code{Labels} attribute of \code{d} when it has one.}
}
\value{
An object of class \code{hclust}.
}
\description{
A drop-in replacement for \code{\link[stats:hclust]{stats::hclust()}} that does not stop at 65536
observations, and clusters at a fraction of the memory.
}
\details{
Clustering consumes its input as scratch, and R's value semantics forbid writing
to the caller's vector, so \code{d} is copied once. Clustering straight from a score
matrix with \code{\link[=nblast_hclust]{nblast_hclust()}} avoids that copy entirely, and is the better route
when you have the matrix to hand.
}
\examples{
d <- dist(matrix(rnorm(40), 20, 2))
h <- fast_hclust(d, method = "average")
# Same tree as stats::hclust for the equivalent method.
all.equal(h$height, hclust(d, method = "average")$height)

}
