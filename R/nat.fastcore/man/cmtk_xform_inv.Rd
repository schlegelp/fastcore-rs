% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cmtk.R
\name{cmtk_xform_inv}
\alias{cmtk_xform_inv}
\title{Apply a CMTK registration to points, backwards}
\usage{
cmtk_xform_inv(
  reg,
  xyz,
  transform = c("warp", "affine"),
  initial_guess = NULL,
  max_iter = 50L,
  tolerance = 1e-09,
  accuracy = 0.001,
  clamp_to_domain = TRUE,
  fallback_to_affine = FALSE,
  invert = FALSE,
  n_cores = NULL,
  progress = FALSE
)
}
\arguments{
\item{reg}{A \code{cmtk_registration}, from \code{\link[=cmtk_read]{cmtk_read()}}.}

\item{xyz}{An \verb{(N, 3)} matrix of coordinates (a data frame or a length-3 vector is
also accepted). \code{nat::xyzmatrix()} gives you this for a neuron.}

\item{transform}{\code{"warp"} (default) applies the full transformation; \code{"affine"}
applies only the affine component. A registration with no spline warp uses its
affine either way.}

\item{initial_guess}{An \verb{(N, 3)} matrix of starting points for the solver. Defaults to
\code{xyz} itself, which is a good guess for any well-behaved registration. In a chain,
this seeds only the first solve.}

\item{max_iter}{Maximum Gauss-Newton iterations per point.}

\item{tolerance}{Stop once the step falls below this.}

\item{accuracy}{Accept a solution only if its residual is within this of the target;
otherwise the row is \code{NaN}.}

\item{clamp_to_domain}{Confine the iterate to the registration's domain box.

\strong{This is what makes the result agree with CMTK.} Turning it off finds preimages
that lie outside the image domain, where \code{streamxform} reports failure -- so you
will get finite numbers where CMTK gives you none.}

\item{fallback_to_affine}{Replace rows the solver could not land with the \emph{inverse} affine
result rather than \code{NaN} -- the mirror of the same argument on \code{\link[=cmtk_xform]{cmtk_xform()}}, with the
same \code{"chain"} (default) and \code{"hop"} semantics.}

\item{invert}{The same per-hop flags as on \code{\link[=cmtk_xform]{cmtk_xform()}}, composed with this whole-chain
inversion: hop \code{i} runs \emph{forwards} here exactly when \code{invert[i]} is \code{TRUE}.}

\item{n_cores}{Number of threads. \code{NULL} (default) uses all cores.}

\item{progress}{Show a progress bar.}
}
\value{
An \verb{(N, 3)} matrix. Rows that did not converge are \code{NaN}.
}
\description{
The affine part is inverted exactly. The spline warp has no closed-form inverse and
is solved per point by damped Gauss-Newton against the analytic Jacobian.
}
\details{
Points whose inverse does not converge come back as \code{NaN}. This is deliberate and
matches CMTK, which reports such points as \code{FAILED} -- a registration is only defined
over a finite domain, and some points simply have no preimage inside it.
}
\examples{
\dontrun{
reg <- cmtk_read("JFRC2_FCWB.list")
pts <- rbind(c(50, 50, 50), c(100, 100, 20))
back <- cmtk_xform_inv(reg, cmtk_xform(reg, pts))
stopifnot(all.equal(back, pts, tolerance = 1e-4))
}

}
\seealso{
\code{\link[=cmtk_read]{cmtk_read()}}, \code{\link[=cmtk_xform]{cmtk_xform()}}
}
