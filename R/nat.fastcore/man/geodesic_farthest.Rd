% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{geodesic_farthest}
\alias{geodesic_farthest}
\title{Distance to the farthest target for each source.}
\usage{
geodesic_farthest(parents, sources, targets, weights, directed)
}
\arguments{
\item{parents}{Integer vector of 0-based parent indices (roots are \verb{< 0}).}

\item{sources}{Optional integer vector of source node indices; \code{NULL} uses
every node.}

\item{targets}{Optional integer vector of target node indices; \code{NULL} uses
every node.}

\item{weights}{Optional numeric vector of edge weights; \code{NULL} counts edges.}

\item{directed}{Logical; if \code{TRUE} only traverse edges child-to-parent. With
non-negative weights the farthest such target is the target ancestor closest
to the root.}
}
\value{
List with \code{distances} (numeric, distance to the farthest target) and
\code{farthest} (integer target index, \code{-1} when unreachable).
}
\description{
The mirror image of \code{geodesic_nearest}: same linear-time algorithm, but it keeps
the farthest rather than the nearest target and never materialises the full
distance matrix. Returns a list with \code{distances} (distance to the farthest
target) and \code{farthest} (index of that target); sources without a reachable
target get \code{-1}. A source that is itself a target is matched to the farthest
\emph{other} target, never to itself.
}
