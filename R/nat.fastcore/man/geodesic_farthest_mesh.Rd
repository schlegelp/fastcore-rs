% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{geodesic_farthest_mesh}
\alias{geodesic_farthest_mesh}
\title{Distance to the farthest target vertex, for each source vertex of a mesh.}
\usage{
geodesic_farthest_mesh(
  faces,
  n_vertices,
  vertices,
  sources,
  targets,
  limit,
  threads
)
}
\arguments{
\item{faces}{Integer or numeric \verb{(F, 3)} matrix of triangle vertex indices.}

\item{n_vertices}{Integer; total number of vertices in the mesh.}

\item{vertices}{Optional numeric \verb{(V, 3)} matrix of vertex coordinates; \code{NULL}
counts edges.}

\item{sources}{Optional integer vector of source vertex indices; \code{NULL} uses
every vertex.}

\item{targets}{Optional integer vector of target vertex indices; \code{NULL} uses
every vertex.}

\item{limit}{Optional numeric; ignore targets further away than this.}

\item{threads}{Optional integer; number of threads. \code{NULL} uses all cores.}
}
\value{
A list with \code{distances} and \code{farthest}.
}
\description{
The mirror image of \code{geodesic_nearest_mesh}, with the same \code{O(sources)} memory
footprint. Unlike \code{nearest}, this cannot stop early — it has to settle every
target — but the farthest one then comes for free, because the search settles
vertices in increasing order of distance.
}
\details{
Returns a list with \code{distances} and \code{farthest}. Sources with no reachable target
get \code{-1}. A source that is itself a target is matched to a \emph{distinct} target.
}
