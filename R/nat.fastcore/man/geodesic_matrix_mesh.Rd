% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{geodesic_matrix_mesh}
\alias{geodesic_matrix_mesh}
\title{Geodesic ("along-the-mesh-edge") distances on a triangle mesh.}
\usage{
geodesic_matrix_mesh(
  faces,
  n_vertices,
  vertices,
  sources,
  targets,
  limit,
  threads
)
}
\arguments{
\item{faces}{Integer or numeric \verb{(F, 3)} matrix of triangle vertex indices
(0-based).}

\item{n_vertices}{Integer; total number of vertices in the mesh.}

\item{vertices}{Optional numeric \verb{(V, 3)} matrix of vertex coordinates. If
given, edges are weighted by their euclidean length; if \code{NULL}, every edge has
weight 1 and the result is a hop count.}

\item{sources}{Optional integer vector of source vertex indices; \code{NULL} uses
every vertex.}

\item{targets}{Optional integer vector of target vertex indices; \code{NULL} uses
every vertex.}

\item{limit}{Optional numeric; ignore vertices further away than this.}

\item{threads}{Optional integer; number of threads. \code{NULL} uses all cores.}
}
\value{
Numeric matrix of geodesic distances (sources in rows, targets in
columns). Unreachable pairs are \code{-1}.
}
\description{
The mesh counterpart to \code{geodesic_distances}, which works on skeletons. A
skeleton is a tree, so distances there come from walking to the lowest common
ancestor; a mesh is a general cyclic graph, so this runs one Dijkstra per source
(or a BFS when unweighted), in parallel.
}
\details{
Note this is the distance \emph{along mesh edges}, not the exact surface geodesic:
paths are constrained to run along edges, so on a coarse mesh they overshoot.

Beware the size of the output: a full \verb{V x V} matrix is ~107 GB at V = 164k. Use
\code{sources} and/or \code{targets} — unlike \code{scipy}'s Dijkstra, passing \code{targets} here
means only those columns are ever allocated.
}
