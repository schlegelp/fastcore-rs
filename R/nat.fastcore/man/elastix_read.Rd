% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/elastix.R
\name{elastix_read}
\alias{elastix_read}
\title{Read an Elastix transform}
\usage{
elastix_read(path, invert = FALSE)
}
\arguments{
\item{path}{Path to a \code{TransformParameters.*.txt} file. Pass several to build a chain, applied
in order: \code{xyz -> path[1] -> path[2] -> ... -> output}.}

\item{invert}{Logical, recycled over \code{path}. \code{TRUE} traverses that transform backwards. Useful
when routing through a bridging graph, where an edge may be traversed either way.}
}
\value{
An object of class \code{elastix_transform}.
}
\description{
Reads an Elastix \code{TransformParameters} file, ready to apply to points. \strong{Elastix itself does
not need to be installed} -- there is no call out to \code{transformix}, no subprocess and no
temporary directory.
}
\details{
A \code{TransformParameters} file is already a chain: its \code{InitialTransformParametersFileName} is
followed recursively, resolved relative to \emph{that file's own directory}. So a four-deep
affine -> affine -> B-spline -> B-spline stack loads from its outermost file alone.

The transform is parsed once and can then be applied any number of times, which is what you
want when transforming every neuron in a dataset.
}
\examples{
\dontrun{
xf <- elastix_read("TransformParameters.FixedFANC.txt")
xyzmatrix(n) <- elastix_xform(xf, xyzmatrix(n))
}

}
\seealso{
\code{\link[=elastix_xform]{elastix_xform()}}, \code{\link[=elastix_xform_inv]{elastix_xform_inv()}}
}
