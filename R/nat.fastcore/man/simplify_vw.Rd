% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{simplify_vw}
\alias{simplify_vw}
\title{Drop the nodes that contribute least area, by Visvalingam-Whyatt.}
\usage{
simplify_vw(
  parents,
  x,
  y,
  z,
  min_area,
  preserve = NULL,
  weights = NULL,
  threads = NULL
)
}
\arguments{
\item{parents}{Integer vector of 0-based parent indices (roots are \verb{< 0}).}

\item{x, y, z}{Numeric vectors of node coordinates, one entry per node.}

\item{min_area}{Numeric; remove a node while the triangle it forms with its two
surviving neighbours is smaller than this, in the \emph{squared} units of the
coordinates. \code{0} or less is a no-op.}

\item{preserve}{Optional logical vector, one entry per node, marking extra nodes that
must survive; \code{NULL} for none.}

\item{weights}{Optional numeric vector of child-to-parent edge weights; \code{NULL}
returns no \code{weights}.}

\item{threads}{Optional integer; number of threads. \code{NULL} uses all cores.}
}
\value{
List with \code{nodes}, \code{parents}, \code{weights} and \code{node_map}, as
\code{downsample_skeleton()}.
}
\description{
The other geometry-aware thinning. Where \code{simplify_rdp()} asks how far the path
\emph{moves}, this asks how much area each node adds to it and repeatedly removes whichever
node adds least. The difference shows under aggressive simplification: RDP will
happily keep one spike and flatten everything around it, while Visvalingam-Whyatt
sheds detail evenly and so keeps a neurite looking like itself.
}
