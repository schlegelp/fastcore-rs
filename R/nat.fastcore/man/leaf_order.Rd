% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clustering.R
\name{leaf_order}
\alias{leaf_order}
\title{Dendrogram leaf order}
\usage{
leaf_order(x)
}
\arguments{
\item{x}{An \code{\link[stats:hclust]{stats::hclust()}} object, or its \verb{(n - 1, 2)} merge matrix: negative
entries are observations (\code{-j} is observation \code{j}), positive entries are the
cluster formed at that earlier step.}
}
\value{
An integer vector of length \code{n} holding each observation's position in
the drawing order, left to right -- a permutation of \code{1:n}, in the same form
as \code{hclust$order}.
}
\description{
The order to place the leaves in so a dendrogram draws without crossing
branches -- what \code{\link[stats:hclust]{stats::hclust()}} returns in its \code{order} element, computed from
a merge matrix alone.
}
\details{
You do not need this for an untouched \code{hclust}: it already carries the same
ordering. It is for a merge matrix you built or edited yourself, where \code{order}
is missing or no longer matches -- a dendrogram drawn with an ordering from a
\emph{different} child order is the crossing-branches mess this exists to avoid.

The walk is iterative, so a 200k-observation chain does not need 200k nested
calls.
}
\examples{
set.seed(1)
h <- fast_hclust(dist(matrix(rnorm(20), 10, 2)), method = "average")
identical(leaf_order(h), h$order)

}
