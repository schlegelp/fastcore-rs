% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{geodesic_matrix_graph}
\alias{geodesic_matrix_graph}
\title{Geodesic distances over an arbitrary graph given as an edge list.}
\usage{
geodesic_matrix_graph(
  edges,
  n_nodes,
  weights,
  directed,
  sources,
  targets,
  limit,
  threads
)
}
\arguments{
\item{edges}{Integer or numeric \verb{(E, 2)} matrix of edges (0-based node indices).}

\item{n_nodes}{Integer; total number of nodes.}

\item{weights}{Optional numeric vector with one length per edge; \code{NULL} counts
edges. Must be finite and non-negative. Parallel edges collapse to the
shortest.}

\item{directed}{Logical; if \code{TRUE} an edge \verb{(u, v)} may only be traversed from
\code{u} to \code{v}.}

\item{sources}{Optional integer vector of source node indices; \code{NULL} uses every
node.}

\item{targets}{Optional integer vector of target node indices; \code{NULL} uses every
node.}

\item{limit}{Optional numeric; ignore nodes further away than this.}

\item{threads}{Optional integer; number of threads. \code{NULL} uses all cores.}
}
\value{
Numeric matrix of geodesic distances (sources in rows, targets in
columns). Unreachable pairs are \code{-1}.
}
\description{
The general form of \code{geodesic_matrix_mesh}. Unlike \code{geodesic_distances}, this
makes no tree assumption — cycles are fine.
}
