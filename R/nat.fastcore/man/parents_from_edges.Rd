% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{parents_from_edges}
\alias{parents_from_edges}
\title{Orient a graph into a rooted spanning forest — one parent per node.}
\usage{
parents_from_edges(
  edges,
  n_nodes,
  weights = NULL,
  roots = NULL,
  precision = 32
)
}
\arguments{
\item{edges}{Integer or numeric \verb{(E, 2)} matrix of edges (0-based node indices).
Direction is ignored.}

\item{n_nodes}{Integer; total number of nodes. Nodes named by no edge are isolated
roots.}

\item{weights}{Optional numeric vector, one length per edge. \code{NULL} gives the
breadth-first tree; weights give the shortest-path tree, which is a different
(and generally deeper) spanning tree. Neither is the minimum spanning tree — for
that, run \code{minimum_spanning_tree} first and orient the edges it keeps.}

\item{roots}{Optional integer vector of 0-based nodes to root at; \code{NULL} roots each
component at its lowest node index. Components holding none of \code{roots} fall back
to that, so the result is always a complete forest. Two roots in the \emph{same}
component split it into two trees, each node going to whichever root is nearer.}

\item{precision}{Integer; 32 or 64, the width the search accumulates at. Nothing
here returns a distance, so this changes only which answer close ties resolve to.}
}
\value{
List with \code{parents} (integer 0-based parent index per node, \code{-1} at a root)
and \code{order} (integer 0-based node indices in the order they settled — a node
always follows its parent, so relabelling by it guarantees parents get lower
indices than their children, which is exactly the SWC requirement).
}
\description{
\code{minimum_spanning_tree} picks \emph{which} edges survive; this picks which way they
point, which is what turns a bag of undirected edges into something you can walk,
root, or write out as SWC. Cycles are fine — each component contributes a spanning
tree of itself, so this doubles as a cycle-breaker.
}
\details{
One search covers the whole graph. The obvious construction — a shortest-path tree
per component — costs \code{O(components * n_nodes)} in output alone, which on a mesh
that shatters into thousands of specks is gigabytes to answer a question whose
answer is one vector.
}
