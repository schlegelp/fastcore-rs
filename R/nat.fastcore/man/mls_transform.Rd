% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/warp.R
\name{mls_transform}
\alias{mls_transform}
\title{Build a moving least squares transform from landmark pairs}
\usage{
mls_transform(source, target, direction = c("forward", "inverse"))
}
\arguments{
\item{source}{An \verb{(M, 3)} matrix of source landmarks. A data frame is also accepted -- if
it has x/y/z columns (in any case) those are used, whatever else it carries, otherwise
the first three columns.}

\item{target}{An \verb{(M, 3)} matrix of target landmarks, one per source landmark.}

\item{direction}{\code{"forward"} (default), or \code{"inverse"} to treat the target as the source
and vice versa. Note this fits the warp in the opposite direction; it is not an exact
inverse, which moving least squares does not have. It is free, though -- no refit.}
}
\value{
An object of class \code{mls_transform}.
}
\description{
The affine flavour of the algorithm published in Schaefer et al. (2006). Unlike a
thin-plate spline there is no fit: every point gets its \emph{own} affine, solved on the fly
from all landmarks weighted by inverse squared distance. Construction is therefore free
and \code{\link[=mls_xform]{mls_xform()}} is the entire cost.
}
\details{
Results are similar to but not identical with \code{\link[=tps_transform]{tps_transform()}}; which suits a given
pair of spaces is worth checking empirically.
}
\examples{
src <- rbind(c(0, 0, 0), c(10, 10, 10), c(100, 100, 100), c(80, 10, 30))
trg <- rbind(c(1, 15, 5), c(9, 18, 21), c(80, 99, 120), c(5, 10, 80))
mls <- mls_transform(src, trg)
mls

# a point sitting on a landmark maps to its partner
stopifnot(all.equal(mls_xform(mls, src), trg, tolerance = 1e-6))

# and the inverse direction maps them back
back <- mls_transform(src, trg, direction = "inverse")
stopifnot(all.equal(mls_xform(back, trg), src, tolerance = 1e-6))

}
\references{
Schaefer S, McPhail T, Warren J (2006). "Image deformation using moving least squares."
\emph{ACM Transactions on Graphics} 25(3):533-540. \doi{10.1145/1141911.1141920}
}
\seealso{
\code{\link[=mls_xform]{mls_xform()}}, \code{\link[=tps_transform]{tps_transform()}}
}
