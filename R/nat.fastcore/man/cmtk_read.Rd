% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cmtk.R
\name{cmtk_read}
\alias{cmtk_read}
\title{Read a CMTK registration}
\usage{
cmtk_read(path)
}
\arguments{
\item{path}{Path to a CMTK \code{.list} registration directory, or to a \code{registration}
file itself (plain or gzipped). Pass a character vector of several to build a
chain, applied in order: \code{points -> path[1] -> path[2] -> ... -> output}.

Direction is \strong{not} fixed here. It is chosen per call -- see the \code{invert} argument of
\code{\link[=cmtk_xform]{cmtk_xform()}}, and \code{\link[=cmtk_xform_inv]{cmtk_xform_inv()}} -- so one object serves every direction and the
registration is parsed only once.}
}
\value{
An object of class \code{cmtk_registration}.
}
\description{
Reads a CMTK registration (a 12-DOF affine plus, usually, a cubic B-spline warp)
ready to be applied to points with \code{\link[=cmtk_xform]{cmtk_xform()}} and \code{\link[=cmtk_xform_inv]{cmtk_xform_inv()}}.
}
\details{
Unlike \code{nat}'s CMTK support, this does \strong{not} shell out to CMTK's \code{streamxform}
binary: CMTK does not need to be installed. Results match \code{streamxform} to ~5e-7 --
its own printing precision -- including which points it declines to transform.

The registration is parsed once, here, and can then be applied as often as you like.
}
\examples{
\dontrun{
reg <- cmtk_read("JFRC2_FCWB.list")
reg

# a chain: A -> B -> C, where the second registration is stored as C->B
chain <- cmtk_read(c("A_B.list", "C_B.list"))
cmtk_xform(chain, pts, invert = c(FALSE, TRUE))
}

}
\seealso{
\code{\link[=cmtk_xform]{cmtk_xform()}}, \code{\link[=cmtk_xform_inv]{cmtk_xform_inv()}}
}
