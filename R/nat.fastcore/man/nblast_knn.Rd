% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nblast.R
\name{nblast_knn}
\alias{nblast_knn}
\title{k nearest neighbours under NBLAST, without building the score matrix}
\usage{
nblast_knn(
  points,
  vects,
  target = NULL,
  target_vects = NULL,
  k = 20L,
  symmetry = c("mean", "forward", "min", "max"),
  n_candidates = 200L,
  alphas = NULL,
  target_alphas = NULL,
  smat = NULL,
  dist_edges = NULL,
  dot_edges = NULL,
  normalize = TRUE,
  limit_dist = NULL,
  voxel = 20,
  n_dirs = 3L,
  splat = TRUE,
  n_cores = NULL,
  precision = 32L,
  progress = FALSE
)
}
\arguments{
\item{points}{List of \verb{(N, 3)} numeric matrices of query point coordinates,
one per neuron.}

\item{vects}{List of \verb{(N, 3)} numeric matrices of unit tangent vectors,
aligned with \code{points}.}

\item{target}{Optional list of \verb{(N, 3)} target point matrices. When given,
neighbours are searched among the targets and the returned indices address
\emph{them}; this is the k-NN counterpart of \code{\link[=nblast]{nblast()}}. \code{NULL} (the default)
runs the all-by-all form over \code{points}, excluding self-matches.}

\item{target_vects}{Tangent vectors for \code{target}; required when \code{target} is
given.}

\item{k}{Integer; neighbours to return per neuron.}

\item{symmetry}{How the two directions of a pair are combined \emph{before} the
top-\code{k} cut. This matters more than for a full matrix: with a matrix you can
symmetrise afterwards against the transpose, but once only \code{k} neighbours per
row are kept the transpose is gone. The asymmetry is real -- a small neuron
contained in a large one scores high one way and low the other -- so \code{"mean"}
is the default. \code{"forward"} keeps each row's own forward score.}

\item{n_candidates}{Integer; shortlist size per neuron, and the one
recall/cost knob. Measured recall@20 on 163,976 real neurons: 0.91 at 50,
0.97 at 100, 0.99 at 200, 0.996 at 400. The budget needed to hold a given
recall grows only about logarithmically with the number of neurons.}

\item{alphas, target_alphas}{Optional lists of per-point alpha (anisotropy)
vectors; \code{NULL} disables alpha weighting.}

\item{smat}{Optional scoring matrix, or \code{NULL} for the built-in FCWB matrix.}

\item{dist_edges, dot_edges}{Bin edges for \code{smat}; required when it is given.}

\item{normalize}{Logical; divide each score by the query self-match score, so
a perfect self-match is 1.}

\item{limit_dist}{Optional numeric distance cut-off; \code{NULL} disables it and
\code{"auto"} takes it from the scoring matrix.}

\item{voxel}{Numeric; signature voxel edge, in the units of \code{points} (um for
the FCWB matrix). 10-20 measured equivalently.}

\item{n_dirs}{Integer; tangent-direction bins for the signature (1 disables
them). \code{3} is the cheap "which axis dominates" case; larger values use a
Fibonacci half-sphere.}

\item{splat}{Logical; trilinearly spread each point over its 8 surrounding
voxels. Worth about 0.05 recall@20.}

\item{n_cores}{Optional integer thread count; \code{NULL} or \verb{<= 0} uses all cores.}

\item{precision}{Integer; 32 or 64, the width scores are returned at. The
scoring maths always runs in double precision.}

\item{progress}{Logical; display a progress bar.}
}
\value{
A list with \code{idx}, an integer \verb{(n_query, k)} matrix of \strong{1-based}
neighbour indices in descending score order, and \code{scores}, the matching
numeric matrix. Rows with fewer than \code{k} available neighbours are padded
with \code{NA} in both.
}
\description{
At connectomics scale an all-by-all is the wrong shape for a k-NN question:
164k neurons is 2.7e10 pairs and a 107 GB matrix, even though the answer
wanted from it is a small k-NN graph (typically to feed a UMAP embedding).
This computes that graph directly, in three stages: each neuron becomes a
coarse voxel-occupancy signature; the \code{n_candidates} most similar neurons per
row are shortlisted from those signatures; and the \strong{exact} NBLAST score is
then computed for the shortlisted pairs only.
}
\details{
The pre-filter is sound because the FCWB scoring matrix has finite support --
beyond its last distance bin (40 um) every cell is about -10, so neurons that
do not overlap in space score at that floor whatever their shape. Only \emph{which}
neurons make the shortlist is approximate; every returned score is an exact
NBLAST value, because a neuron belonging in the true top-\code{k} outranks the
global k-th and so cannot be dropped by the rerank once shortlisted.

Measured on 163,976 zebrafish neurons: recall@20 = 0.99 at the default
\code{n_candidates = 200}, scoring 0.16\% of the pairs and taking about 5 minutes
against an estimated 35 hours for the exact all-by-all.
}
\examples{
set.seed(1)
pts <- lapply(1:6, function(i) {
  base <- matrix(rnorm(60), ncol = 3)
  base + matrix(rep(c(i, 0, 0), each = 20), ncol = 3)
})
vects <- lapply(pts, function(p) {
  v <- matrix(0, nrow(p), 3)
  v[, 1] <- 1
  v
})
nn <- nblast_knn(pts, vects, k = 2)
nn$idx
nn$scores
}
\seealso{
\code{\link[=nblast_allbyall]{nblast_allbyall()}} for the full matrix, \code{\link[=nblast_hclust]{nblast_hclust()}} to cluster
one.
}
