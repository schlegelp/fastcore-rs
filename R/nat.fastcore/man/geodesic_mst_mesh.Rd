% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{geodesic_mst_mesh}
\alias{geodesic_mst_mesh}
\title{Minimum spanning tree over a subset of mesh vertices, by geodesic distance.}
\usage{
geodesic_mst_mesh(
  faces,
  n_vertices,
  nodes,
  vertices = NULL,
  limit = NULL,
  threads = NULL
)
}
\arguments{
\item{faces}{Integer or numeric \verb{(F, 3)} matrix of triangle vertex indices
(0-based).}

\item{n_vertices}{Integer; total number of vertices.}

\item{nodes}{Integer vector of 0-based vertices to span. Must be distinct.}

\item{vertices}{Optional numeric \verb{(V, 3)} matrix of vertex coordinates. If given,
edges are weighted by their euclidean length; if \code{NULL}, distances are hop
counts.}

\item{limit}{Optional numeric; do not join vertices further apart than this. The
result is then a \emph{forest} when that disconnects the subset. Unlike a matrix
route's limit this also prunes the sweep, so it buys time rather than merely
discarding results.}

\item{threads}{Optional integer; number of threads. \code{NULL} uses all cores.}
}
\value{
List with \code{edges} (integer \verb{(M, 2)} matrix of \strong{0-based positions in
\code{nodes}}, not vertex indices — so \code{nodes[edges + 1]} maps back — ascending by
weight) and \code{weights} (numeric geodesic distance across each). The returned
weights are exactly the geodesic distances between the pairs they join, so they
are usable as lengths and not merely as an ordering.
}
\description{
The tree that reconnects a scatter of surviving vertices through the mesh they were
carved out of — the last step of a skeletonization, where the mesh has been thinned
to a few thousand vertices that must be rejoined along the surface rather than
through space.
}
\details{
The obvious route is to ask for the \verb{k x k} geodesic matrix and hand it to a matrix
MST. That materialises \code{k^2} distances to use \code{k - 1} of them — 400 MB at
\code{k = 10000}, before the \code{O(k^2)} MST itself — and needs \code{k} separate searches to
fill. This never forms the matrix: following Mehlhorn's distance-network
construction, one multi-source search partitions every vertex by which of \code{nodes}
is nearest, and each mesh edge whose endpoints fall in different cells offers one
candidate. An MST over those is an MST of the full distance network, so one sweep
and one Kruskal replace \code{k} searches and a dense matrix.
}
