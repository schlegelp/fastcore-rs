% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{downsample_skeleton}
\alias{downsample_skeleton}
\title{Keep every \code{factor}-th node of every segment, dropping the rest.}
\usage{
downsample_skeleton(parents, factor, preserve = NULL, weights = NULL)
}
\arguments{
\item{parents}{Integer vector of 0-based parent indices (roots are \verb{< 0}).}

\item{factor}{Integer; keep one node in every \code{factor}, counting from each segment's
distal end. \code{1} keeps everything; the useful range starts at 2.}

\item{preserve}{Optional logical vector, one entry per node, marking extra nodes that
must survive; \code{NULL} for none.}

\item{weights}{Optional numeric vector of child-to-parent edge weights; \code{NULL}
returns no \code{weights}.}
}
\value{
List with \code{nodes} (0-based indices of the surviving nodes, in their original
relative order), \code{parents} (their new 0-based parent indices, \code{-1} for roots,
indexing \emph{into} \code{nodes}) and \code{weights} (length of each node's edge to its new
parent, i.e. the summed length of the chain it replaces; \code{NULL} exactly when
\code{weights} was \code{NULL}). Total cable length is preserved.
}
\description{
The plain "make this skeleton smaller" operation: it pays no attention to geometry,
so reach for it when the skeleton is already evenly sampled and you just want fewer
nodes. Roots, branch points and leafs always survive, so the result is still the same
neuron — only its unbranched stretches are sampled \code{factor} times more coarsely. See
\code{simplify_rdp()} and \code{simplify_vw()} for the geometry-aware alternatives.
}
