% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/elastix.R
\name{elastix_xform_inv}
\alias{elastix_xform_inv}
\title{Invert an Elastix transform on points}
\usage{
elastix_xform_inv(
  xf,
  xyz,
  out_of_bounds = c("identity", "nan"),
  initial_guess = NULL,
  max_iter = 50L,
  seed_iter = 8L,
  tolerance = 1e-09,
  accuracy = 0.001,
  lattice_points = 16000L,
  n_cores = NULL,
  progress = FALSE
)
}
\arguments{
\item{xf}{An \code{elastix_transform}, from \code{\link[=elastix_read]{elastix_read()}}.}

\item{xyz}{An \verb{(N, 3)} matrix, a data frame with three columns, or a bare length-3 vector.}

\item{out_of_bounds}{What to do with points outside a B-spline's control-point grid.
\code{"identity"} (the default) returns them \strong{unchanged}, which is exactly what \code{transformix}
does. \code{"nan"} returns \code{NaN} instead.

The default is silent by nature: a neuron straddling the grid edge comes back partly
transformed and looks perfectly fine. Use \code{"nan"} when you would rather see the boundary
than trust it.}

\item{initial_guess}{An \verb{(N, 3)} matrix of starting points for the solver, in the \emph{source}
space. Rarely needed -- the solver seeds itself -- but it is what breaks the tie where a
target has more than one preimage.}

\item{max_iter}{Solver budget per point.}

\item{seed_iter}{Rounds of the fixed-point pre-seed. Zero starts the solver at the target,
which loses points wherever the deformation is large.}

\item{tolerance}{Step-size convergence threshold.}

\item{accuracy}{Accept a solution only if its residual is within this of the target, in world
units. Otherwise the row is \code{NaN}.}

\item{lattice_points}{Size of the global seed lattice -- the last-resort start for the few
points the cheap seeds fail on. Built once per call and consulted only by points that have
already failed. Set to 0 to disable.}

\item{n_cores}{Cap the thread pool. \code{NULL} uses all cores.}

\item{progress}{Show a progress bar.}
}
\value{
An \verb{(N, 3)} matrix of coordinates in the source space. Rows with no preimage are \code{NaN}.
}
\description{
Elastix itself cannot do this: \code{transformix} only goes forwards, which is why packages that
use Elastix ship two separate registration files per brain pair.
}
\details{
Linear steps are inverted exactly. Each B-spline warp has no closed-form inverse and is solved
per point by damped Gauss-Newton against the analytic Jacobian.

What is guaranteed is \strong{forward-consistency}: \code{elastix_xform(xf, elastix_xform_inv(xf, y))}
returns \code{y}, to within \code{accuracy}. What is \emph{not} guaranteed is that inverting a forward
transform returns the point you started from, because a B-spline warp need not be injective --
a strongly folded registration maps several points to the same place, and no inverse can
recover which one you meant. Points with no preimage at all come back as \code{NaN}.
}
\seealso{
\code{\link[=elastix_xform]{elastix_xform()}}
}
