% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{minimum_spanning_tree}
\alias{minimum_spanning_tree}
\title{Minimum (or maximum) spanning forest of an undirected graph.}
\usage{
minimum_spanning_tree(
  edges,
  n_nodes,
  weights = NULL,
  maximize = FALSE,
  threads = NULL,
  precision = 32
)
}
\arguments{
\item{edges}{Integer or numeric \verb{(E, 2)} matrix of edges (0-based node indices).}

\item{n_nodes}{Integer; total number of nodes.}

\item{weights}{Optional numeric vector, one weight per edge; \code{NULL} treats every
edge as equal. Must be finite; negative weights are allowed.}

\item{maximize}{Logical; return the \emph{maximum} spanning forest instead. This exists
so you do not have to pass \code{1 / weights} to invert the ordering — a transform
that both loses precision and blows up on the zero weights that legitimately
occur.}

\item{threads}{Optional integer; number of threads. \code{NULL} uses all cores.}

\item{precision}{Integer; 32 or 64, the width the search accumulates at. Nothing
here returns a distance, so this changes only which answer close ties resolve to.}
}
\value{
Integer vector of 0-based \strong{row indices into \code{edges}}, ordered by weight —
not the edges themselves, so you can index whatever per-edge data you hold with
the same vector. Remember to add 1 before using it to subset an R matrix.
}
\description{
Kruskal's algorithm on the same Union-Find as the component search: sort the edges
by weight, keep the ones that join two different components. Disconnected input is
fine — each component contributes its own tree.
}
