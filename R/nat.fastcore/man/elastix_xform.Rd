% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/elastix.R
\name{elastix_xform}
\alias{elastix_xform}
\title{Apply an Elastix transform to points}
\usage{
elastix_xform(
  xf,
  xyz,
  out_of_bounds = c("identity", "nan"),
  invert = FALSE,
  n_cores = NULL,
  progress = FALSE
)
}
\arguments{
\item{xf}{An \code{elastix_transform}, from \code{\link[=elastix_read]{elastix_read()}}.}

\item{xyz}{An \verb{(N, 3)} matrix, a data frame with three columns, or a bare length-3 vector.}

\item{out_of_bounds}{What to do with points outside a B-spline's control-point grid.
\code{"identity"} (the default) returns them \strong{unchanged}, which is exactly what \code{transformix}
does. \code{"nan"} returns \code{NaN} instead.

The default is silent by nature: a neuron straddling the grid edge comes back partly
transformed and looks perfectly fine. Use \code{"nan"} when you would rather see the boundary
than trust it.}

\item{invert}{Logical, length 1 or one flag per transform. \code{TRUE} traverses that transform
backwards -- what you need when routing through a bridging graph, where an edge may be walked
in either direction.

This is \strong{not} the same as \code{\link[=elastix_xform_inv]{elastix_xform_inv()}}, which inverts the whole composition
(reversing the order \emph{and} flipping every hop). For a single transform the two agree; for a
chain they do not, and only \code{invert} can express a mixed-direction traversal.}

\item{n_cores}{Cap the thread pool. \code{NULL} uses all cores.}

\item{progress}{Show a progress bar.}
}
\value{
An \verb{(N, 3)} matrix of transformed coordinates.
}
\description{
Apply an Elastix transform to points
}
\examples{
\dontrun{
xf <- elastix_read("TransformParameters.FixedFANC.txt")
elastix_xform(xf, cbind(50, 50, 50))
}

}
\seealso{
\code{\link[=elastix_xform_inv]{elastix_xform_inv()}}
}
