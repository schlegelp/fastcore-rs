% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{set_num_threads}
\alias{set_num_threads}
\title{Set the number of threads used for parallel work in this session.}
\usage{
set_num_threads(n)
}
\arguments{
\item{n}{Integer; number of threads. Must be >= 1.}
}
\value{
\code{NULL}, invisibly. Called for its side effect.
}
\description{
By default nat.fastcore uses every core it can see, which is the right answer
for a single call and the wrong one when the \emph{caller} is already spreading
work over processes (\code{parallel::mclapply()}, \code{future::plan(multisession)}, a
cluster job): each worker would claim every core, and the resulting
oversubscription can make the whole thing slower than running it on one core.
Nothing tells a worker process that it is one of twenty, so it has to be told.
}
\details{
Call this once, before any other nat.fastcore function. The pool is built at
most once per session, by whichever comes first: an earlier
\code{set_num_threads()}, the \code{RAYON_NUM_THREADS} environment variable, a call to
\code{get_num_threads()}, or simply the first parallel call. Calling it again with
the same \code{n} is a no-op; calling it with a different \code{n} is an error, as the
pool cannot be resized.
}
